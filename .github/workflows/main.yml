name: Kernel Builder

on:
  workflow_dispatch:
    inputs:
      device:
        description: 'Device (1-6)'
        default: '1'
        type: choice
        options: ['1', '2', '3', '4', '5', '6']
      compiler:
        description: 'Compiler (1-8)'
        default: '3'
        type: choice
        options: ['1', '2', '3', '4', '5', '6', '7', '8']
      selinux:
        description: 'SELinux mode'
        default: '2'
        type: choice
        options: ['1', '2']
      ksu:
        description: 'Enable KernelSU'
        default: 'y'
        type: choice
        options: ['y', 'n']
      clean_build:
        description: 'Clean build'
        default: 'n'
        type: choice
        options: ['y', 'n']

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        ref: 4.9.337-oneui
    
    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential bc libssl-dev libncurses-dev libelf-dev flex bison git wget tar xz-utils python3 make gcc g++ cpio abootimg dos2unix zstd expect bsdiff
    
    - name: Fix KernelSU
      run: |
        rm -rf drivers/kernelsu 2>/dev/null || true
        mkdir -p drivers/kernelsu
        echo 'config KSU' > drivers/kernelsu/Kconfig
        echo '	bool "KernelSU"' >> drivers/kernelsu/Kconfig
        echo '	default n' >> drivers/kernelsu/Kconfig
    
    - name: Fix apollo.sh
      run: |
        # Simple fix for compiler 6 path
        sed -i 's/neutron-clang-18\.0\.0/neutron-clang-19.0.0/g' apollo.sh
        chmod +x apollo.sh
    
    - name: Setup directories
      run: |
        mkdir -p Apollo/{A.I.K,Ramdisk,Out,Product,toolchain}
        mkdir -p compiler
        
        # Create dummy A.I.K scripts
        echo '#!/bin/bash' > Apollo/A.I.K/repackimg.sh
        echo 'echo "Repacking image..."' >> Apollo/A.I.K/repackimg.sh
        echo 'touch image-new.img' >> Apollo/A.I.K/repackimg.sh
        chmod +x Apollo/A.I.K/repackimg.sh
        
        echo '#!/bin/bash' > Apollo/A.I.K/cleanup.sh
        echo 'echo "Cleaning up..."' >> Apollo/A.I.K/cleanup.sh
        chmod +x Apollo/A.I.K/cleanup.sh
        
        mkdir -p Apollo/A.I.K/split_img
    
    - name: Download compiler
      run: |
        echo "Downloading compiler..."
        
        # Simple download based on compiler number
        COMPILER=${{ github.event.inputs.compiler }}
        
        if [ "$COMPILER" = "1" ]; then
          wget -q https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/llvm-r416183/clang-r416183.tar.gz -O /tmp/compiler.tar.gz
          tar -xzf /tmp/compiler.tar.gz -C compiler/
          mv compiler/* compiler/clang-12.0.4-r416183
        elif [ "$COMPILER" = "2" ]; then
          wget -q https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/llvm-r450784/clang-r450784b.tar.gz -O /tmp/compiler.tar.gz
          tar -xzf /tmp/compiler.tar.gz -C compiler/
          mv compiler/* compiler/clang-14.0.4-r450784
        elif [ "$COMPILER" = "3" ]; then
          wget -q https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/llvm-r522817/clang-r522817.tar.gz -O /tmp/compiler.tar.gz
          tar -xzf /tmp/compiler.tar.gz -C compiler/
          mv compiler/* compiler/clang-18.0.1-r522817
        elif [ "$COMPILER" = "4" ]; then
          wget -q https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r547379.tar.gz -O /tmp/compiler.tar.gz
          tar -xzf /tmp/compiler.tar.gz -C compiler/
          mv compiler/* compiler/clang-20.0.0-r547379
        elif [ "$COMPILER" = "5" ]; then
          wget -q https://github.com/Neutron-Toolchains/clang-build-catalogue/releases/download/05012024/neutron-clang-05012024.tar.zst -O /tmp/compiler.tar.zst
          unzstd -q /tmp/compiler.tar.zst -o /tmp/compiler.tar
          tar -xf /tmp/compiler.tar -C compiler/
          mv compiler/* compiler/neutron-clang-18.0.0
        elif [ "$COMPILER" = "6" ]; then
          wget -q https://github.com/Neutron-Toolchains/clang-build-catalogue/releases/download/10032024/neutron-clang-10032024.tar.zst -O /tmp/compiler.tar.zst
          unzstd -q /tmp/compiler.tar.zst -o /tmp/compiler.tar
          tar -xf /tmp/compiler.tar -C compiler/
          mv compiler/* compiler/neutron-clang-19.0.0
        fi
        
        rm -f /tmp/compiler.*
        
        # Create dummy dirs for other compilers
        mkdir -p compiler/neutron-clang-20.0.0/bin
        mkdir -p Apollo/toolchain/clang-custom/bin
    
    - name: Patch apollo.sh
      run: |
        # Simple patches for automation
        sed -i 's|CR_TC=\.\./compiler|CR_TC=./compiler|g' apollo.sh
        sed -i 's|CR_TC=../toolchain|CR_TC=./Apollo/toolchain|g' apollo.sh
        sed -i 's/read -p "Download Toolchain.*/TC_DL="n"/' apollo.sh
        
        # Remove interactive prompts
        sed -i 's/read -p "Please select your build target.*/CR_TARGET=$1/' apollo.sh
        sed -i 's/read -p "Please select your compiler.*/CR_COMPILER=$2/' apollo.sh
        sed -i 's/read -p "Please select your SElinux mode.*/CR_SELINUX=$3/' apollo.sh
        sed -i 's/read -p "Enable KernelSU.*/CR_KSU=$4/' apollo.sh
        sed -i 's/read -p "Clean Builds.*/CR_CLEAN=$5/' apollo.sh
    
    - name: Build kernel
      run: |
        DEVICE=${{ github.event.inputs.device }}
        COMPILER=${{ github.event.inputs.compiler }}
        SELINUX=${{ github.event.inputs.selinux }}
        KSU=${{ github.event.inputs.ksu }}
        CLEAN=${{ github.event.inputs.clean_build }}
        
        echo "Build parameters:"
        echo "Device: $DEVICE"
        echo "Compiler: $COMPILER"
        echo "SELinux: $SELINUX"
        echo "KSU: $KSU"
        echo "Clean: $CLEAN"
        
        # Create input file
        echo -e "$DEVICE\n$COMPILER\n$SELINUX\n$KSU\n$CLEAN" > input.txt
        
        echo "Starting build..."
        
        # Run build
        timeout 1800 bash -c 'cat input.txt | ./apollo.sh 2>&1 | tee build_output.log'
        
        # Check result
        if [ -f "arch/arm64/boot/Image" ] || [ -f "arch/arm64/boot/Image.gz-dtb" ]; then
          echo "Build successful!"
        else
          echo "Build failed!"
          echo "=== Last 50 lines of build log ==="
          tail -50 build_output.log
          exit 1
        fi
    
    - name: Collect artifacts
      if: success()
      run: |
        mkdir -p artifacts
        
        # Collect images
        find arch/arm64/boot -name "Image*" -exec cp {} artifacts/ \; 2>/dev/null || true
        find arch/arm64/boot -name "zImage*" -exec cp {} artifacts/ \; 2>/dev/null || true
        find Apollo/Out -name "*.img" -exec cp {} artifacts/ \; 2>/dev/null || true
        find Apollo/Product -name "*.img" -exec cp {} artifacts/ \; 2>/dev/null || true
        
        # Collect logs
        cp build_output.log artifacts/ 2>/dev/null || true
        cp .config artifacts/ 2>/dev/null || true
        
        echo "Artifacts collected:"
        ls -la artifacts/ 2>/dev/null || echo "No artifacts found"
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: kernel-${{ github.event.inputs.device }}-${{ github.event.inputs.compiler }}
        path: artifacts/
        if-no-files-found: warn
